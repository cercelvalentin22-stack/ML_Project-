# Outputs folder

The outputs are generated when you run:

```bash
python src/live_market_ai_project.py
```

They are not pre-generated here because the project is designed to pull data until the latest available trading day on the computer where it is run.
